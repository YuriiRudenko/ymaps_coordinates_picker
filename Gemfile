source 'http://rubygems.org'

# Declare your gem's dependencies in gmap_coordinates_picker.gemspec.
# Bundler will treat runtime dependencies like base dependencies, and
# development dependencies will be added by default to the :development group.
gemspec

gem 'jquery-rails' # jquery-rails is used by the dummy application
gem 'coffee-rails' # CoffeeScript adapter for the Rails asset pipeline
gem 'slim'         # template language whose goal is reduce the syntax to the essential parts without becoming cryptic.
gem 'slim-rails'   # Slim templates generator for Rails 3 and 4
